# 抖音(douyin)平台支持 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 potplayer-live 增加抖音直播支持(单房间 `parse` + 分区浏览 `list_category`),复用 serve 自愈中继,不改变虎牙现有行为。

**Architecture:** 新增 `sites/douyin.py` 平台插件;把 `parse`/`list_category` 里可测的部分抽成纯函数(`_parse_enter`/`_parse_rooms`/`resolve_web_rid`/`resolve_partition`),网络壳(ttwid + HTTP)是薄封装、不单测(与虎牙一致)。先做一次派发层去虎牙耦合的小重构(每个 room 带完整 `url`,`/g/` 与 `/category/` 判定下沉到各平台模块),使 cli/sites 平台无关。

**Tech Stack:** Python 3.14、标准库(`urllib`/`json`/`re`)、uv、unittest。

## Global Constraints

- Python 固定 `3.14.*`,用 uv 运行;入口 `uv run -m potplayer_live`。
- 测试:标准库 `unittest`,**不触网**——网络边界(`fetch`)一律做成可注入参数,用假上游驱动。
- 跑测试:`uv run -m unittest tests.test_potplayer`。**提交前必须全绿。**
- 非平凡逻辑改动后补断言;纯函数优先用参考实现/固定 fixture 比对。
- 注释与 commit 用中文,解释「为什么」;最小改动,不做投机性抽象。
- **只用 flv**(server 的 `relay_flv` 只处理 flv,不吃 HLS)。
- 平台插件接口:`DOMAINS`、`PLAY_HEADERS`、`parse(url)->{rid,nick,title,living,streams{名:{quality,url,backups}}}`;分区浏览可选 `list_category(ident,pages)->{name,slug,rooms:[{room,nick,viewers,title,url}]}`。

---

### Task 1: 派发层去虎牙耦合 + room 携带完整 url

把 `/g/` 判定下沉到 huya 模块,`sites.is_category` 改为按平台委派;每个 room 带完整 `url`;cli 用 `r['url']` 不再拼 huya 域名。虎牙行为不变(url 恰好等于原硬编码值)。

**Files:**
- Modify: `potplayer_live/sites/huya.py`(新增 `is_category`;`list_category` 给 room 补 `url`)
- Modify: `potplayer_live/sites/__init__.py`(`is_category` 委派;`list_rooms` 用 `get_site` + `category_slug`)
- Modify: `potplayer_live/cli.py`(`browse_category` 用 `r['url']`)
- Test: `tests/test_potplayer.py`

**Interfaces:**
- Produces:
  - `huya.is_category(url: str) -> bool`(http 路径首段 == `"g"`)
  - `huya.list_category(...)` 返回的每个 room 新增 `url: str`(`"https://www.huya.com/" + room`)
  - `sites.is_category(url)` 保持原对外语义(委派实现);`sites.category_slug(url)` 不变(取 http 路径第 2 段,裸串原样)

- [ ] **Step 1: 写失败测试(huya.is_category + room url)**

在 `tests/test_potplayer.py` 的 `TestListCategory` 类后追加:

```python
class TestHuyaIsCategory(unittest.TestCase):
    def test_g_url_is_category(self):
        self.assertTrue(huya.is_category("https://www.huya.com/g/lol"))

    def test_room_url_not_category(self):
        self.assertFalse(huya.is_category("https://www.huya.com/lpl"))
        self.assertFalse(huya.is_category("https://www.huya.com/660000"))


class TestHuyaRoomUrl(unittest.TestCase):
    CATS = [{"gid": 1, "host": "lol", "name": "英雄联盟", "online": 9}]

    def test_rooms_carry_full_url(self):
        pages = {1: _card("333003", "主播A", "911万", "标题A")}
        res = huya.list_category(
            "lol", pages=1, categories=self.CATS,
            fetch=lambda slug, page: pages.get(page, ""),
        )
        self.assertEqual(res["rooms"][0]["url"], "https://www.huya.com/333003")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run -m unittest tests.test_potplayer.TestHuyaIsCategory tests.test_potplayer.TestHuyaRoomUrl -v`
Expected: FAIL(`module 'huya' has no attribute 'is_category'`;`KeyError: 'url'`)

- [ ] **Step 3: 实现 huya.is_category 并给 room 补 url**

`potplayer_live/sites/huya.py`,在 `list_category` 定义前新增:

```python
def is_category(url: str) -> bool:
    """分区页判定:http 路径首段为 g(如 /g/lol)才算分区浏览。"""
    p = urllib.parse.urlparse(url)
    if p.scheme in ("http", "https"):
        return p.path.strip("/").split("/")[0] == "g"
    return False
```

在 `list_category` 的去重循环里给 room 补完整 url(改动 `for r in page_rooms:` 那段):

```python
        for r in page_rooms:
            if r["room"] not in seen:
                seen.add(r["room"])
                r["url"] = "https://www.huya.com/" + r["room"]  # cli 直接用,不再拼域名
                rooms.append(r)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run -m unittest tests.test_potplayer.TestHuyaIsCategory tests.test_potplayer.TestHuyaRoomUrl -v`
Expected: PASS

- [ ] **Step 5: 派发层委派 + cli 去硬编码(改后跑全量,原有 TestSitesCategory 须仍绿)**

`potplayer_live/sites/__init__.py`,把 `is_category` 改为按平台委派(`category_slug` 保持不变):

```python
def is_category(url: str) -> bool:
    """判断输入意图是否为分区浏览。

    http URL 按域名委派给对应平台模块的 is_category(不支持的平台/房间 URL → False);
    非 http 的裸标识(名/别名/gid)默认走虎牙分区浏览(现状不变)。
    """
    p = urllib.parse.urlparse(url)
    if p.scheme in ("http", "https"):
        try:
            mod = get_site(url)
        except RuntimeError:
            return False
        f = getattr(mod, "is_category", None)
        return bool(f and f(url))
    return bool(url.strip())
```

同文件 `list_rooms` 改为用 `get_site` 派发(裸串仍默认虎牙):

```python
def list_rooms(url: str, pages: int = 3) -> dict:
    """列出分区房间。http URL 按域名派发到对应平台;裸串默认虎牙。"""
    p = urllib.parse.urlparse(url)
    mod = get_site(url) if p.scheme in ("http", "https") else huya
    return mod.list_category(category_slug(url), pages=pages)
```

`potplayer_live/cli.py` 的 `browse_category`:非交互打印行与播放都改用 `r['url']`:

```python
        line = f"  {i:2}. [{r['viewers']:>7}] {r['nick']}  {r['title']}"
        if not interactive:
            line += f"  → {r['url']}"
        print(line)
```
```python
    return play_room(rooms[idx]["url"], a)
```

- [ ] **Step 6: 跑全量确认无回归**

Run: `uv run -m unittest tests.test_potplayer -v`
Expected: PASS(原 `TestSitesCategory` 等全部仍绿)

- [ ] **Step 7: 提交**

```bash
git add potplayer_live/sites/huya.py potplayer_live/sites/__init__.py potplayer_live/cli.py tests/test_potplayer.py
git commit -m "refactor: 派发层去虎牙耦合,room 带完整 url,分区判定下沉到平台模块

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 抖音单房间 parse

新增 `sites/douyin.py`:纯函数 `resolve_web_rid` + `_parse_enter`(测),网络壳 `_ttwid`/`parse`(不测,薄封装),并注册进 `SITES`。

**Files:**
- Create: `potplayer_live/sites/douyin.py`
- Modify: `potplayer_live/sites/__init__.py`(`from . import huya, douyin`;`SITES = [huya, douyin]`)
- Test: `tests/test_potplayer.py`

**Interfaces:**
- Consumes: `common.http_get`
- Produces:
  - `douyin.DOMAINS = ["live.douyin.com", "douyin.com"]`、`douyin.PLAY_HEADERS`
  - `douyin.resolve_web_rid(url: str) -> str`(路径最后一段)
  - `douyin._parse_enter(payload: dict, web_rid: str) -> dict`(`{rid,nick,title,living,streams}`)
  - `douyin.parse(url: str) -> dict`(网络壳)

- [ ] **Step 1: 写失败测试(_parse_enter + resolve_web_rid)**

在 `tests/test_potplayer.py` 顶部已 `from potplayer_live.sites import huya`,补一行 `from potplayer_live.sites import douyin`。在文件末尾 `if __name__` 之前追加:

```python
import json as _json


def _enter(status=2, with_sdk=True):
    room = {"status": status, "title": "早安", "owner": {"nickname": "主播A"}}
    stream_url = {
        "flv_pull_url": {"FULL_HD1": "http://x/full.flv", "HD1": "http://x/hd.flv"}
    }
    if with_sdk:
        stream_url["live_core_sdk_data"] = {
            "pull_data": {
                "options": {
                    "qualities": [
                        {"name": "原画", "sdk_key": "origin", "v_bit_rate": 0},
                        {"name": "高清", "sdk_key": "sd", "v_bit_rate": 1000},
                    ]
                },
                "stream_data": _json.dumps(
                    {
                        "data": {
                            "origin": {"main": {"flv": "http://x/origin.flv"}},
                            "sd": {"main": {"flv": "http://x/sd.flv"}},
                        }
                    }
                ),
            }
        }
    room["stream_url"] = stream_url
    return {"data": {"data": [room], "user": {"nickname": "主播A"}}}


class TestDouyinResolveWebRid(unittest.TestCase):
    def test_last_path_segment(self):
        self.assertEqual(
            douyin.resolve_web_rid("https://live.douyin.com/123456"), "123456"
        )


class TestDouyinParseEnter(unittest.TestCase):
    def test_living_with_sdk_qualities(self):
        info = douyin._parse_enter(_enter(), "123456")
        self.assertTrue(info["living"])
        self.assertEqual(info["rid"], "123456")
        self.assertEqual(info["nick"], "主播A")
        self.assertEqual(info["title"], "早安")
        self.assertEqual(
            info["streams"]["原画"], {"quality": 0, "url": "http://x/origin.flv", "backups": []}
        )
        self.assertEqual(info["streams"]["高清"]["quality"], 1000)
        self.assertEqual(info["streams"]["高清"]["url"], "http://x/sd.flv")

    def test_not_living_empty_streams(self):
        info = douyin._parse_enter(_enter(status=0), "1")
        self.assertFalse(info["living"])
        self.assertEqual(info["streams"], {})

    def test_fallback_to_flv_pull_url(self):
        info = douyin._parse_enter(_enter(with_sdk=False), "1")
        self.assertTrue(info["living"])
        self.assertEqual(info["streams"]["原画"]["url"], "http://x/full.flv")
        self.assertEqual(info["streams"]["高清"]["url"], "http://x/hd.flv")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run -m unittest tests.test_potplayer.TestDouyinParseEnter -v`
Expected: FAIL(`No module named 'potplayer_live.sites.douyin'` 或属性缺失)

- [ ] **Step 3: 实现 sites/douyin.py**

创建 `potplayer_live/sites/douyin.py`:

```python
#!/usr/bin/env python3
"""抖音(live.douyin.com)平台解析模块。

接口见 sites/__init__.py。单房间走 web enter 接口(只需 ttwid cookie);
分区浏览走 partition/detail/room 接口。只用 flv(serve 中继不吃 HLS)。
"""

import re
import json
import urllib.parse
import urllib.request

from ..common import http_get

DOMAINS = ["live.douyin.com", "douyin.com"]
UA_DESKTOP = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
)
REFERER = "https://live.douyin.com/"
PLAY_HEADERS = {"User-Agent": UA_DESKTOP, "Referer": REFERER}

# enter 接口(只带 ttwid cookie 即可,先不做 a_bogus 签名)
ENTER_URL = (
    "https://live.douyin.com/webcast/room/web/enter/?aid=6383&app_name=douyin_web"
    "&live_id=1&device_platform=web&language=zh-CN&enter_from=web_live"
    "&cookie_enabled=true&browser_platform=Win32&browser_name=Chrome"
    "&browser_version=120.0.0.0&web_rid={web_rid}&room_id_str=&enter_source="
)

# flv_pull_url 无 sdk 清晰度名时的兜底名映射(抖音键名固定)
_FALLBACK_NAMES = {"FULL_HD1": "原画", "HD1": "高清", "SD1": "标清", "SD2": "流畅"}


def resolve_web_rid(url: str) -> str:
    """从房间地址取 web_rid(路径最后一段)。"""
    return urllib.parse.urlparse(url).path.strip("/").split("/")[-1]


def _ttwid() -> str:
    """GET 首页从 Set-Cookie 抓 ttwid(抖音 web 接口所需)。"""
    req = urllib.request.Request(REFERER, headers={"User-Agent": UA_DESKTOP})
    with urllib.request.urlopen(req, timeout=15) as r:
        cookies = r.headers.get_all("Set-Cookie") or []
    for c in cookies:
        m = re.search(r"ttwid=([^;]+)", c)
        if m:
            return m.group(1)
    return ""


def _parse_enter(payload: dict, web_rid: str) -> dict:
    """把 enter 接口 JSON 解析成房间信息。纯函数,便于不触网测试。

    清晰度优先取 live_core_sdk_data(带中文名 + 码率),回退 flv_pull_url。只收 flv。
    """
    d = payload.get("data", {}) or {}
    rooms = d.get("data") or []
    room = rooms[0] if rooms else {}
    user = d.get("user", {}) or {}
    info = {
        "rid": web_rid,
        "nick": user.get("nickname") or room.get("owner", {}).get("nickname"),
        "title": room.get("title"),
        "living": room.get("status") == 2,
        "streams": {},
    }
    if not info["living"]:
        return info
    su = room.get("stream_url", {}) or {}
    sdk = su.get("live_core_sdk_data", {}).get("pull_data", {})
    quals = sdk.get("options", {}).get("qualities") or []
    if quals and sdk.get("stream_data"):
        flv_map = json.loads(sdk["stream_data"]).get("data", {})
        for q in quals:
            url = flv_map.get(q.get("sdk_key"), {}).get("main", {}).get("flv")
            if url:
                info["streams"][q["name"]] = {
                    "quality": q.get("v_bit_rate", 0),
                    "url": url,
                    "backups": [],
                }
    else:  # 兜底:flv_pull_url 键名 → 中文名,码率未知记 0
        for key, url in (su.get("flv_pull_url") or {}).items():
            info["streams"][_FALLBACK_NAMES.get(key, key)] = {
                "quality": 0,
                "url": url,
                "backups": [],
            }
    return info


def parse(url: str) -> dict:
    """解析抖音房间(网络壳:取 ttwid → 请求 enter → _parse_enter)。"""
    web_rid = resolve_web_rid(url)
    headers = {"User-Agent": UA_DESKTOP, "Referer": REFERER, "Cookie": f"ttwid={_ttwid()}"}
    payload = json.loads(http_get(ENTER_URL.format(web_rid=web_rid), headers=headers))
    return _parse_enter(payload, web_rid)
```

`potplayer_live/sites/__init__.py` 顶部改为:

```python
from . import huya, douyin

# 已支持的平台模块(按需追加)
SITES = [huya, douyin]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run -m unittest tests.test_potplayer.TestDouyinResolveWebRid tests.test_potplayer.TestDouyinParseEnter -v`
Expected: PASS

- [ ] **Step 5: 加派发路由测试并跑全量**

追加(验证域名路由到抖音):

```python
class TestDouyinDispatch(unittest.TestCase):
    def test_get_site_routes_to_douyin(self):
        self.assertIs(sites.get_site("https://live.douyin.com/123456"), douyin)

    def test_play_headers_has_referer(self):
        h = sites.play_headers("https://live.douyin.com/123456")
        self.assertEqual(h["Referer"], "https://live.douyin.com/")
```

Run: `uv run -m unittest tests.test_potplayer -v`
Expected: PASS(全绿)

- [ ] **Step 6: 提交**

```bash
git add potplayer_live/sites/douyin.py potplayer_live/sites/__init__.py tests/test_potplayer.py
git commit -m "feat(douyin): 单房间 parse(enter 接口 + ttwid,只收 flv)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 抖音分区浏览 list_category

纯函数 `_parse_rooms`/`resolve_partition`(测)+ `is_category`(测)+ 网络壳 `list_category`(注入 `fetch` 后可测翻页去重)。

**Files:**
- Modify: `potplayer_live/sites/douyin.py`
- Test: `tests/test_potplayer.py`

**Interfaces:**
- Consumes: `douyin._ttwid`、`common.http_get`
- Produces:
  - `douyin.is_category(url) -> bool`(http 路径首段 == `"category"`)
  - `douyin.resolve_partition(ident) -> (partition:int, partition_type:int, name:str)`
  - `douyin._parse_rooms(payload: dict) -> list[{room,nick,viewers,title,url}]`
  - `douyin.list_category(ident, pages=3, count=15, fetch=None) -> {name,slug,rooms}`;`fetch(partition,partition_type,offset)->dict` 可注入

- [ ] **Step 1: 写失败测试**

追加:

```python
class TestDouyinIsCategory(unittest.TestCase):
    def test_category_url(self):
        self.assertTrue(douyin.is_category("https://live.douyin.com/category/720,1"))

    def test_room_url_not_category(self):
        self.assertFalse(douyin.is_category("https://live.douyin.com/123456"))


class TestDouyinResolvePartition(unittest.TestCase):
    def test_id_type_pair(self):
        self.assertEqual(douyin.resolve_partition("720,1"), (720, 1, "720,1"))

    def test_alias(self):
        # ALIASES 命中(用其中一个真实别名;若表为空则本用例应更新)
        self.assertIn("英雄联盟", douyin.ALIASES)
        p, t, name = douyin.resolve_partition("英雄联盟")
        self.assertEqual((p, t), douyin.ALIASES["英雄联盟"])
        self.assertEqual(name, "英雄联盟")

    def test_unknown_raises(self):
        with self.assertRaises(RuntimeError):
            douyin.resolve_partition("不存在的分区")


def _dy_room(web_rid, nick, viewers, title):
    return {
        "web_rid": web_rid,
        "room": {
            "title": title,
            "owner": {"nickname": nick},
            "stats": {"total_user_str": viewers},
        },
    }


class TestDouyinListCategory(unittest.TestCase):
    def test_parse_rooms_fields_and_url(self):
        payload = {"data": {"data": [_dy_room("111", "n1", "1.2万", "t1")]}}
        self.assertEqual(
            douyin._parse_rooms(payload),
            [{"room": "111", "nick": "n1", "viewers": "1.2万", "title": "t1",
              "url": "https://live.douyin.com/111"}],
        )

    def test_dedup_across_pages_preserves_order(self):
        pages = {
            0: {"data": {"data": [_dy_room("a", "na", "9", "ta"), _dy_room("b", "nb", "8", "tb")]}},
            15: {"data": {"data": [_dy_room("b", "nb", "8", "tb"), _dy_room("c", "nc", "7", "tc")]}},
        }
        res = douyin.list_category(
            "720,1", pages=3, count=15,
            fetch=lambda p, t, offset: pages.get(offset, {"data": {"data": []}}),
        )
        self.assertEqual([r["room"] for r in res["rooms"]], ["a", "b", "c"])
        self.assertEqual(res["slug"], "720,1")

    def test_stops_on_empty_page(self):
        res = douyin.list_category(
            "720,1", pages=5, count=15,
            fetch=lambda p, t, offset: {"data": {"data": [_dy_room("a", "n", "9", "t")]}}
            if offset == 0 else {"data": {"data": []}},
        )
        self.assertEqual([r["room"] for r in res["rooms"]], ["a"])
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run -m unittest tests.test_potplayer.TestDouyinIsCategory tests.test_potplayer.TestDouyinResolvePartition tests.test_potplayer.TestDouyinListCategory -v`
Expected: FAIL(属性缺失)

- [ ] **Step 3: 实现分区相关代码**

`potplayer_live/sites/douyin.py` 追加(`ROOM_URL` 常量放模块顶部常量区,其余函数放文件末尾):

顶部常量区新增:

```python
ROOM_URL = "https://live.douyin.com/{web_rid}"

# 分区房间列表接口(非 v2,ttwid-only)
PARTITION_URL = (
    "https://live.douyin.com/webcast/web/partition/detail/room/?aid=6383"
    "&app_name=douyin_web&live_id=1&device_platform=web&count={count}"
    "&offset={offset}&partition={partition}&partition_type={ptype}&req_from=2"
)

# 中文别名 → (partition, partition_type)。抖音分区 id 需抓包校准,可能随平台调整。
# 未命中的分区请用 id,type 形式(如 .../category/720,1)。
ALIASES = {
    "英雄联盟": (2701, 1),
    "王者荣耀": (694, 1),
    "和平精英": (2354, 1),
}
```

文件末尾新增:

```python
def is_category(url: str) -> bool:
    """分区页判定:http 路径首段为 category(如 /category/720,1)。"""
    p = urllib.parse.urlparse(url)
    if p.scheme in ("http", "https"):
        return p.path.strip("/").split("/")[0] == "category"
    return False


def resolve_partition(ident):
    """把分区标识解析成 (partition, partition_type, 显示名)。

    "720,1" → 直接拆;中文别名 → 查 ALIASES;都不中 → 抛带引导的错误。
    """
    ident = str(ident).strip()
    if "," in ident:
        p, t = ident.split(",", 1)
        return int(p), int(t), ident
    if ident in ALIASES:
        p, t = ALIASES[ident]
        return p, t, ident
    raise RuntimeError(
        f"未知抖音分区「{ident}」。请用 id,type 形式,如 "
        "https://live.douyin.com/category/720,1"
    )


def _parse_rooms(payload: dict) -> list:
    """从分区接口 JSON 提取房间卡片。纯函数,便于不触网测试。

    web_rid 可能在 item 顶层或 room/owner 下;人气取 total_user_str。
    """
    out = []
    for it in payload.get("data", {}).get("data") or []:
        room = it.get("room", {}) or {}
        owner = room.get("owner", {}) or {}
        web_rid = it.get("web_rid") or room.get("web_rid") or owner.get("web_rid")
        if not web_rid:
            continue
        out.append(
            {
                "room": str(web_rid),
                "nick": owner.get("nickname"),
                "viewers": room.get("stats", {}).get("total_user_str"),
                "title": room.get("title"),
                "url": ROOM_URL.format(web_rid=web_rid),
            }
        )
    return out


def list_category(ident, pages=3, count=15, fetch=None):
    """列出抖音分区在播房间(跨页按 web_rid 去重保序)。

    fetch(partition, partition_type, offset)->dict 可注入,便于不触网测试。
    """
    partition, ptype, name = resolve_partition(ident)
    if fetch is None:
        ttwid = _ttwid()

        def fetch(partition, ptype, offset):
            headers = {"User-Agent": UA_DESKTOP, "Referer": REFERER,
                       "Cookie": f"ttwid={ttwid}"}
            url = PARTITION_URL.format(
                count=count, offset=offset, partition=partition, ptype=ptype
            )
            return json.loads(http_get(url, headers=headers))

    seen, rooms = set(), []
    for i in range(pages):
        try:
            payload = fetch(partition, ptype, i * count)
        except Exception:
            break
        page_rooms = _parse_rooms(payload)
        if not page_rooms:
            break  # 该页无房间 → 到底
        for r in page_rooms:
            if r["room"] not in seen:
                seen.add(r["room"])
                rooms.append(r)
    return {"name": name, "slug": ident, "rooms": rooms}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run -m unittest tests.test_potplayer.TestDouyinIsCategory tests.test_potplayer.TestDouyinResolvePartition tests.test_potplayer.TestDouyinListCategory -v`
Expected: PASS

- [ ] **Step 5: 加分区派发路由测试**

追加(验证 sites 层对抖音 category URL 的路由):

```python
class TestDouyinCategoryDispatch(unittest.TestCase):
    def test_sites_is_category_delegates(self):
        self.assertTrue(sites.is_category("https://live.douyin.com/category/720,1"))
        self.assertFalse(sites.is_category("https://live.douyin.com/123456"))

    def test_category_slug_extracts_ident(self):
        self.assertEqual(
            sites.category_slug("https://live.douyin.com/category/720,1"), "720,1"
        )
```

Run: `uv run -m unittest tests.test_potplayer -v`
Expected: PASS(全绿)

- [ ] **Step 6: 提交**

```bash
git add potplayer_live/sites/douyin.py tests/test_potplayer.py
git commit -m "feat(douyin): 分区浏览 list_category(id,type + 别名,翻页去重)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 收尾——文档与全量回归

更新用户可见文档(cli docstring / README 若有),跑全量,人工校准提示。

**Files:**
- Modify: `potplayer_live/cli.py`(`url` 参数 help 举例补抖音)
- Modify: `README.md`(若存在:支持平台列表补抖音;分区用法举例)
- Test: `tests/test_potplayer.py`(全量)

**Interfaces:**
- Consumes: 前三个 Task 的产物,无新增接口。

- [ ] **Step 1: 更新 cli 参数示例**

`potplayer_live/cli.py` 的 `ap.add_argument("url", help=...)` 里补抖音示例:

```python
    ap.add_argument(
        "url",
        help="直播间地址(虎牙 https://www.huya.com/lpl、抖音 https://live.douyin.com/123456),"
        "或分区(虎牙 /g/lol、英雄联盟、lol;抖音 https://live.douyin.com/category/720,1)",
    )
```

- [ ] **Step 2: 更新 README(若存在)**

Run: `ls README.md 2>/dev/null && echo has_readme || echo no_readme`

若 `has_readme`:在支持平台/用法处补一行抖音示例(单房间地址与 `.../category/<id>,<type>` 分区)。若 `no_readme`:跳过本步。

- [ ] **Step 3: 跑全量确认全绿**

Run: `uv run -m unittest tests.test_potplayer -v`
Expected: PASS(全部用例)

- [ ] **Step 4: 人工校准清单(记录,不阻塞提交)**

在 commit message 或一条说明里标注需真机验证的点(ponytail:留调参旋钮):
- enter 接口 ttwid-only 是否被 403(若是→评估补 `a_bogus`)。
- `_parse_enter` 里 `data.user.nickname` / `stream_url` 键路径与真实响应一致性。
- `_parse_rooms` 的 `web_rid`/`stats.total_user_str` 键路径与真实分区响应一致性。
- `ALIASES` 里的分区 id 用真实抓包校准。

- [ ] **Step 5: 提交**

```bash
git add potplayer_live/cli.py tests/test_potplayer.py README.md 2>/dev/null
git commit -m "docs(douyin): cli/README 补抖音用法示例;标注真机校准点

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage:**
- `sites/douyin.py` DOMAINS/PLAY_HEADERS/parse → Task 2 ✓
- ttwid 获取 → Task 2 `_ttwid` ✓
- enter 解析(在播/标题/主播/清晰度/回退)→ Task 2 `_parse_enter` + 测试 ✓
- a_bogus 风险(先 ttwid-only,不预实现)→ Task 2 实现说明 + Task 4 校准清单 ✓
- list_category(id,type + 别名 + 翻页去重 + room 带 url)→ Task 3 ✓
- 派发层去耦(room 带 url、/g/ 与 /category/ 下沉、cli 用 r['url'])→ Task 1 + Task 3 ✓
- 登记 SITES → Task 2 ✓
- 测试(parse/list_category/派发/虎牙回归)→ 各 Task Step ✓
- 只用 flv → `_parse_enter`/`_parse_rooms` 只取 flv,无 HLS ✓

**Placeholder scan:** 无 TBD/TODO;所有 code step 均给出完整代码;测试均含具体断言。`ALIASES` 的分区 id 是需真机校准的真实值(非占位),已在 Task 4 校准清单标注。

**Type consistency:** `_parse_enter(payload, web_rid)`、`_parse_rooms(payload)`、`resolve_partition(ident)->(int,int,str)`、`list_category(ident,pages,count,fetch)`、`is_category(url)->bool` 在定义(Task 2/3)与测试引用处签名一致;room dict 字段 `{room,nick,viewers,title,url}` 在 huya(Task 1)与 douyin(Task 3)一致,cli 消费 `r['url']`/`r['viewers']`/`r['nick']`/`r['title']` 均已产出。
